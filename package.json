{
  "name": "@snowstorm/root",
  "private": "true",
  "version": "0.0.0-beta.1",
  "author": "<mail@henrygressmann.de>",
  "scripts": {
    "build": "yarn workspace @snowstorm/core run build && yarn workspace @snowstorm/cli run build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "@explodingcamera/eslint-config": "^15.1.0",
    "eslint": "^7.22.0",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0"
  }
}
