{
  "name": "@snowstorm/root",
  "version": "0.0.0-beta.1",
  "private": "true",
  "license": "MIT",
  "author": "<mail@henrygressmann.de>",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "run-s -l build:*",
    "build:core": "yarn workspace @snowstorm/core run build ",
    "build:cli": "yarn workspace @snowstorm/cli run build",
    "watch": "run-p -l watch:*",
    "watch:core": "yarn workspace @snowstorm/core run watch",
    "watch:cli": "yarn workspace @snowstorm/cli run watch"
  },
  "dependencies": {
    "@explodingcamera/eslint-config": "^15.1.0",
    "eslint": "^7.22.0",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
