{
  "name": "@snowstorm/router",
  "version": "0.8.0",
  "license": "MIT",
  "author": "<mail@henrygressmann.de>",
  "type": "module",
  "files": [
    "lib/"
  ],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --splitting --chunk-names=chunks/[name]-[hash] --outdir=lib --platform=node --target=node16 --format=esm",
    "watch": "npm run build -- --watch",
    "test": "tap --ts --node-arg=--loader=ts-node/esm"
  },
  "dependencies": {
    "deepmerge": "^4.2.2"
  },
  "devDependencies": {
    "@types/tap": "^15.0.5",
    "esbuild": "^0.14.2",
    "tap": "^15.1.6",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
